name: Docs CI\non:\n  push:\n    paths:\n      - '**/*.md'\n      - 'docs/meta/registry.yaml'\n      - 'scripts/**'\njobs:\n  validate:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - name: Validate YAML\n        run: scripts/doc_validate.sh || true\n      - name: Doc Sync\n        run: scripts/doc_sync.sh || true\n      - name: Markdown Validate\n        run: scripts/md_validate.sh || true
