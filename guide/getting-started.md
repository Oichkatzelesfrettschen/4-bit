# Getting Started

## Prerequisites

- Rust nightly (see `rust-toolchain.toml` for exact version)
- cargo-llvm-cov (optional, for coverage)

## Building

```bash
cargo build --workspace
```

## Running Tests

```bash
cargo test --workspace
```

## Development

{{#include ../docs/DEVELOPMENT.md}}
