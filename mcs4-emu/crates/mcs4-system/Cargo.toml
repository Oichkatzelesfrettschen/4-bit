[package]
name = "mcs4-system"
description = "System integration for MCS-4 emulator"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
mcs4-core = { path = "../mcs4-core" }
mcs4-bus = { path = "../mcs4-bus" }
mcs4-chips = { path = "../mcs4-chips" }
tracing.workspace = true
rayon.workspace = true
memmap2.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true

[dev-dependencies]
tempfile.workspace = true
# `bumpalo` is only used in tests (`src/lib.rs::wiring_tests`); demoted from
# `[dependencies]` to `[dev-dependencies]` during debt phase D3.4 after
# `cargo +nightly udeps --workspace` flagged it as unused outside tests.
bumpalo.workspace = true
